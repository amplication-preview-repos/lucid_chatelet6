apiVersion: v1
kind: Service
metadata:
  name: {{ include "online-course-service.fullname" . }}
  labels:
    {{- include "online-course-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "online-course-service.selectorLabels" . | nindent 4 }}
